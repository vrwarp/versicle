import React from 'react';
import { renderToString } from 'react-dom/server';
import { FileUploader } from './src/components/library/FileUploader';
// A trick to see if we can just test the component rendering directly.
