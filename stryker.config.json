{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment": "Scoped mutation testing over the sync domain \u2014 see vitest.mutation.config.ts for why the scope is narrowed and why `include` is replaced rather than merged (mergeConfig concatenates arrays, which silently runs the whole 3,300-test suite once per mutant). 60.0% total / 72.6% covered. break=58 ratchets the total; raise it, never lower it.",
  "packageManager": "npm",
  "testRunner": "vitest",
  "vitest": {
    "configFile": "vitest.mutation.config.ts"
  },
  "reporters": [
    "html",
    "clear-text",
    "progress"
  ],
  "htmlReporter": {
    "fileName": "reports/mutation/index.html"
  },
  "coverageAnalysis": "perTest",
  "concurrency": 3,
  "timeoutMS": 30000,
  "mutate": [
    "src/domains/sync/**/*.ts",
    "src/app/sync/**/*.ts",
    "!src/**/*.{test,spec}.{ts,tsx}",
    "!src/domains/sync/backend/Mock*.ts"
  ],
  "thresholds": {
    "high": 90,
    "low": 70,
    "break": 58
  },
  "tempDirName": ".stryker-tmp",
  "cleanTempDir": true
}
