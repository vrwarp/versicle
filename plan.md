1. Modify `src/components/library/FileUploader.tsx`.
2. Locate the "Connect Google Drive" button.
3. Update the button children to render "Connecting..." text and `aria-hidden="true"` on the spinner when `isDriveConnecting` is true, improving accessibility and providing better interaction feedback.
4. Run tests and linting.
5. Submit the changes.
