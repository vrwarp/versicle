# Test Fixtures

This directory contains static files used as test data for unit tests.

*   **`alice.epub`**: A sample EPUB file used to verify the functionality of the ingestion service and parsing logic in a controlled environment.
