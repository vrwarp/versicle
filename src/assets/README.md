# Assets

This directory contains static assets that are imported directly into the TypeScript code (e.g., via `import logo from './assets/logo.svg'`).

## Contents

*   **`react.svg`**: The React logo, used in the initial project template.
